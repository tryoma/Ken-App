import { MdKeyboardDoubleArrowDown } from 'react-icons/md';

const LoadMoreIcon = ({ size }: { size: string }) => {
  return <MdKeyboardDoubleArrowDown size={size} />;
};

export default LoadMoreIcon;
